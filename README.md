# graph_flat_norm
The flat norm can be understood as a low curvature approximation to as set. When applied to objects like images the flat norm is essentially a denoising algorithm, features with sharp edges or noise (i.e. high curvature) get removed. 
